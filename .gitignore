/target
/result
/result-*
.direnv/
*.wav
/dist

# Editor and desktop cruft, so a contributor's tools do not land in a PR.
.DS_Store
.vscode/
.idea/
*.swp
*~

# Pointing the build at a local starkit checkout. Per-machine, and a
# committed one would send CI to a path that does not exist there.
.cargo/
